# 全局原图转可编辑 PPTX 规范 v3

本规范对本项目内任何原图、截图或图片型幻灯片长期生效，不限于当前 9 张原图。机器可读的唯一参数源是 `config/qwen-global-policy.json`；运行前必须执行 `tools/check_qwen_global_policy.py`。

## 工作台与并发路线

每页必须保留六个审核画布：`SOURCE_REFERENCE`、`BG_CLEAN`、`OCR_LAYER`、`NONTEXT_LAYER`、`COMPOSITE`、`DIFF_HEATMAP`。OCR 路线与视觉/非文本路线并发建立清单，但只有两份清单都完成后才能建立联合前景掩膜。生产幻灯片只能包含 `BG_CLEAN + 可移动装饰 + 原生非文本 + 图库/Qwen 资产 + OCR 文本`；整张原图只允许进入隐藏参考页或审计资源。

## 清洁背景的可证明条件

`BG_CLEAN` 必须删除所有 OCR 字形、语义非文本、可移动装饰及其阴影/抗锯齿边缘；经审核的固定纯装饰可以保留。清洁器只能修改联合掩膜内部像素，掩膜外像素同一率和 SSIM 均不得低于 0.995，平均 DeltaE 不得高于 2。通过还必须满足：OCR 复扫为 0、文字残边不高于 0.005、非文本残边不高于 0.01、阴影残留不高于 0.02、装饰完整度不低于 0.95、接缝审核通过。整页重绘被禁止；复杂区域只能进入 Qwen Image 掩膜内局部修复。

## 图库执行规则

原图裁片只可用于检索查询。图库预览 PNG 只可用于候选展示和视觉复核，不得替代真实资产：原生组件必须实例化组件或复制形状 XML，SVG 必须插入 SVG，PNG/Qwen 资产必须是独立透明素材。候选先取 Top-5，方向、开口、柱数、内部结构、Logo 图文任一不符均一票否决；0.95 以上可直接候选，0.88–0.95 必须渲染复核，低于 0.88 转 Qwen Image。所有插入资产仍须做局部渲染审核。

## 独立门禁与修复顺序

文字层须达到视觉 0.90、内容 0.995、关键文本 1.0、对象覆盖 1.0；任何错别字或 OCR 错误均不通过。非文本层须达到视觉 0.80、主要元素召回 0.95、全部元素召回 0.90，并满足坐标、大小、宽高比、结构和层级门禁。背景、文字、非文本必须分别通过后才能融合；融合及移动/删除测试通过后才允许导出。

修复顺序固定为：遗漏元素、错误分类/匹配、坐标和大小、颜色和样式、层级遮挡、文字与非文本融合。每轮按从上到下、同一视觉带从左到右扫描。六轮是升级处理方法的触发点，不是自动通过条件；无法达标时状态必须是 `needs_review`，不得交付为合格结果。

## 主要模块

- `lib/reconstruction-v3.ts`：文字、非文本、背景、融合和可编辑性硬门禁。
- `lib/gallery-asset-policy.ts`：图库实际资产类型与预览 PNG 禁用规则。
- `lib/rebuild-protocol.ts`：v3 工作台阶段与渲染审核契约。
- `tools/vision/build_clean_background.py`：联合掩膜和确定性优先的 BG_CLEAN 候选。
- `tools/vision/audit_clean_background.py`：掩膜外同一性、OCR 复扫、残边、接缝和装饰审计。
- `tools/qa/audit_editability_manifest.py`：整张原图、对象树、移动/删除和前景单独渲染审计。
- `app/api/export-pptx/route.ts`：最终硬门禁；缺少任一证据即拒绝导出。
